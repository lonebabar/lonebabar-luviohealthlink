function returnReport(){const p=LH.findPatient(lPatient.value.trim());if(!p)return alert("Patient not found.");LH.update(p.id,x=>{const r=x.reports.find(z=>z.test.toLowerCase()===lTest.value.trim().toLowerCase());if(r){r.status="Report Available";r.lab="Luvio Diagnostics"}else x.reports.unshift({test:lTest.value,lab:"Luvio Diagnostics",date:new Date().toLocaleDateString("en-IN"),status:"Report Available"})});alert("Report returned to the patient's record.")}
