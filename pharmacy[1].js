function dispense(){const p=LH.findPatient(pPatient.value.trim());if(!p)return alert("Patient not found.");const m=p.medicines.find(x=>x.name.toLowerCase()===pMedicine.value.trim().toLowerCase());if(m)m.status="Dispensed";else p.medicines.unshift({name:pMedicine.value,dose:"As prescribed",frequency:"As prescribed",duration:"As prescribed",doctor:"Luvio Doctor",date:new Date().toLocaleDateString("en-IN"),status:"Dispensed"});LH.savePatient(p);alert("Dispensing recorded in the patient's medication history.")}
